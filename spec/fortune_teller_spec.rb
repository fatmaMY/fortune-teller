require "spec_helper"

RSpec.describe FortuneTeller do
  it "has a version number" do
    expect(FortuneTeller::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
